<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday Prince! 🎉</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            text-align: center;
            background: #000;
            color: white;
            overflow: hidden;
            transition: background 1s;
        }
        .cake-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            cursor: pointer;
            flex-direction: column;
            background-color: #fff7d1;
            position: relative;
        }

        /* Styling for "Don't Tap on Cake" text */
        .dont-tap-text {
            font-family: 'Algerian';
            font-size: 1rem;
            color: darkgoldenrod; /* Tomato red */
            text-shadow: 30px 3px 8px rgba(0, 0, 0, 0.5);
            letter-spacing: 2px;
            animation: glowText 1.1s ease-in-out infinite alternate;
        }

        @keyframes glowText {
            from { text-shadow: 0 0 10px rgba(255,255,255,0.6); }
            to { text-shadow: 0 0 20px rgba(255,255,255,1); }
        }
        .cake {
            width: 250px;
            animation: shakeCake 2s ease-in-out infinite; /* Shake for 2 seconds loop */
        }

        @keyframes shakeCake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-10px); }
            50% { transform: translateX(10px); }
            75% { transform: translateX(-5px); }
        }

        .hidden {
            display: none;
        }

        .birthday-container {
            display: none;
            background: linear-gradient(135deg, #ff7f50, #ff1493);
            background-size: 400% 400%;
            animation: gradientBG 10s infinite alternate;
            height: 100vh;
            text-align: center;
            padding: 20px;
            box-sizing: border-box;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }

        h1 {
            font-size: 2rem;
            color: #fff;
            text-shadow: 0 0 10px rgba(255,255,255,0.8);
            animation: glow 2s infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 10px rgba(255,255,255,0.6); }
            to { text-shadow: 0 0 20px rgba(255,255,255,1); }
        }

        .photo {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 5px solid white;
            box-shadow: 0 0 15px rgba(255,255,255,0.6);
        }

        .message {
            font-size: 0.8rem;
            margin-top: 10px;
            font-weight: bold;
            color: #fff;
            text-shadow: 0 0 10px rgba(0,0,0,0.7);
            line-height: 1.5;
            background-color: rgba(0,0,0,0.5);
            padding: 10px;
            border-radius: 10px;
            width: 80%;
            margin: 20px auto;
        }

        .footer {
            margin-top: 120px;
            font-size: 0.9rem;
            color: brown;
            font-weight: bold;
            text-transform: uppercase;
            text-shadow: 0 0 10px rgba(255,255,255,0.8);
        }

        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            background: white;
            opacity: 0.7;
            top: -10px;
            animation: fall 5s linear infinite;
        }

        @keyframes fall {
            from { top: -10px; transform: rotate(0deg); }
            to { top: 100vh; transform: rotate(360deg); }
        }
    </style>
</head>
<body>

    <!-- First Screen: Burning Cake -->
    <div class="cake-container" id="cakeScreen" onclick="revealBirthdayPage()">
        <img class="cake" src="cake.png" alt="Birthday Cake">
        <p class="dont-tap-text">Don't Tap on Cake! 🎂</p>
    </div>

    <!-- Second Screen: Birthday Surprise -->
    <div class="birthday-container hidden" id="birthdayScreen">
        <audio autoplay loop id="bgMusic">
            <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-2.mp3" type="audio/mp3">
        </audio>

        <h1 style="color: red">Happy Birthday</h1>
        <h1 style="color: palegoldenrod">Prince</h1>
        
        
        <img class="photo" src="img.jpg" alt="Prince's Photo">
        <p class="message">
            "Prince, tera saath ho toh har din hai special,<br />
Teri dosti meri zindagi ka hai ek treasure,<br />
Tere janamdin par yeh dua hai meri,<br /><br />
Khush rahe tu hamesha, mera dost,<br /> mere Prince

"  
        </p>
        <p class="footer">By Rajeev 💖</p>
    </div>

    <script>
        function revealBirthdayPage() {
            document.getElementById("cakeScreen").style.display = "none";
            document.getElementById("birthdayScreen").style.display = "block";
            document.body.style.background = "linear-gradient(45deg, #ff9a9e, #fad0c4, #fad0c4, #ffdde1)";
            createConfetti();
            document.getElementById("bgMusic").play();
        }

        function createConfetti() {
            for (let i = 0; i < 100; i++) {
                let confetti = document.createElement("div");
                confetti.classList.add("confetti");
                confetti.style.left = Math.random() * 100 + "vw";
                confetti.style.animationDuration = (Math.random() * 3 + 2) + "s";
                confetti.style.background = `hsl(${Math.random() * 360}, 100%, 75%)`;
                document.body.appendChild(confetti);
            }
        }
    </script>

</body>
</html>
